debug:
  script:
    - ./gradlew clean assemble
  except:
    - /^v.*$/

bintray:
  script:
    - ./gradlew clean assembleRelease publishMavenAndroidPublicationToMavenLocal bintrayUpload
  only:
    - /^v.*$/